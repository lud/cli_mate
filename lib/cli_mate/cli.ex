defmodule CliMate.CLI do
  use CliMate

  @moduledoc """
  This module provides the CLI tools defined by `CliMate` without needing to
  `use CliMate` in you own project.

  Note that this module would not be available for a mix task distributed with
  hex.
  """
end
